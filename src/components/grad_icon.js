import React, { Component } from "react";
import grad from "../graduation-cap-32.ico";

class GradIcon extends Component {
  render() {
    return (
      <div>
        <div class="image-center">
            <img src={grad} />
        </div>
      </div>
    );
  }
}

export default GradIcon;
